# Milvus 人脸向量库部署（face_db-deploy）
# 复制为 .env 后按需修改

# Milvus 对外端口
MILVUS_PORT=19530
MILVUS_METRICS_PORT=9091

# 宿主机数据目录（持久化）
MILVUS_DATA_HOST_PATH=./data/milvus

# Docker 网络名；须与 app/app-deploy 中 FACE_MILVUS_NETWORK 一致
FACE_MILVUS_NETWORK=face-milvus-stack
