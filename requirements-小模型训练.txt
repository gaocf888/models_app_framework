# 小模型训练
#
# 说明：
# - 本文件聚焦「小模型（CV/检测/分类/分割等）训练」相关依赖；
# - 小模型在线推理/通道管理依赖放在 `requirements-小模型应用.txt`。

# === 深度学习基础框架 ===
torch>=2.2.0           # 小模型训练主框架（PyTorch），用于定义网络结构与训练循环
torchvision>=0.17.0    # 常用视觉模型与数据增强组件（ResNet/YOLO 系列实现通常依赖）

# === 训练过程可视化与监控 ===
tensorboard>=2.16.0    # 训练指标可视化（loss/accuracy 等），与日志目录配合使用

# === 推理部署格式支持（按需启用） ===
onnxruntime>=1.17.0    # 如需将训练模型导出为 ONNX 并在推理服务中使用，可安装该运行时
# tensorrt             # 若使用 TensorRT 进行高性能推理，可以在有 NVIDIA 环境的机器上单独安装

# === YOLOv8 训练（Ultralytics） ===
ultralytics>=8.0.0     # YOLOv8 训练与权重产出（app/train/yolo/yolo_train.py 使用）
pyyaml>=6.0.0          # 读取 app/train/yolo/config.yaml / data.yaml（配合训练脚本配置）
