# 大模型训练：Code+LoRA 微调（及可选 LLaMA-Factory）
#
# 说明：
# - 本文件聚焦「大模型训练/微调」相关依赖；
# - 在线推理与业务 API 依赖放在 `requirements-大模型应用.txt`。

# === HuggingFace & LoRA 微调所需依赖 ===
transformers>=4.45.0
datasets>=2.18.0
peft>=0.10.0
accelerate>=0.28.0
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.66.0

# === Qwen-VL 辅助（VLM 训练按需安装） ===
# qwen-vl-utils>=0.0.8

# === 训练过程日志与可视化（按需） ===
tensorboard>=2.16.0
