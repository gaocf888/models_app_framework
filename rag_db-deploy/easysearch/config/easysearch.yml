cluster.name: ${EASYSEARCH_CLUSTER_NAME}
node.name: ${EASYSEARCH_NODE_NAME}
network.host: 0.0.0.0

# 关键：把当前单节点声明为初始 master
# cluster.initial_master_nodes: ["${EASYSEARCH_NODE_NAME}"]

# Single-node mode for app-level RAG deployment.
# discovery.type: single-node

# Keep defaults simple; resource tuning should be done by env vars and host sizing.
action.destructive_requires_name: true
