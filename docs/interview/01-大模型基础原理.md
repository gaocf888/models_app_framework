# 01 · 大模型基础原理

> 面试定位：把「模型怎么算、怎么表示、为什么能生成」讲清楚。应用岗也要能讲到 KV Cache / 上下文长度。  
> 预计：60~90 分钟。  
> 本项目锚点：对话走 Decoder-only 补全；上下文预算必须对齐 vLLM `--max-model-len`。

---

## 0. 30 秒能讲清楚

当代主流 LLM（Qwen / Llama / DeepSeek）是 **Decoder-only Transformer**：把文本切成 token，每个 token 变成向量，用 **自注意力** 看前面所有 token，再经 FFN 变换，最后预测下一个 token。训练目标是「给定上文，最大化下一个 token 概率」。推理就是把这个过程 **自回归** 重复下去。

---

## 1. Token 与词表

| 概念 | 面试怎么说 |
|------|------------|
| Token | 模型的最小输入单位，不是「字」也不是固定「词」 |
| BPE / Byte-level BPE | 高频子词合并；英文常见，对未知词友好 |
| SentencePiece / Unigram | 多语言、中文常用路线之一 |
| 词表大小 | 过大浪费嵌入矩阵；过小则长文本 token 数暴涨 |

**中文面试点**：同样一句话，不同 tokenizer 的 token 数差很大 → 计费、上下文占用、时延都受影响。长上下文优化首先是 **少 token**（摘要、裁剪、RAG 只塞相关片段）。

**特殊 token**：BOS/EOS、PAD、对话角色标记（`<|im_start|>` 等）。Chat 模板（Chat Template）必须和训练时一致，否则「模型变笨」其实是格式错了。

---

## 2. Transformer 积木（必须能画）

一层 Decoder 块（现代常见变体）：

```text
x → RMSNorm → Attention(Q,K,V) → +残差
  → RMSNorm → FFN(SwiGLU 等)  → +残差
```

和原版论文的差异（常被追问）：

| 原版 Transformer | 现代 LLM 常见做法 |
|------------------|-------------------|
| Post-Norm | Pre-Norm / RMSNorm，训练更稳 |
| 绝对正弦位置编码 | RoPE 旋转位置编码 |
| ReLU FFN | SwiGLU / GeGLU |
| Encoder-Decoder | Decoder-only（GPT 路线） |
| 稠密 | 可选 MoE（专家稀疏激活） |

### 2.1 Self-Attention（核心公式直觉）

对每个位置：

- \(Q = x W_Q,\ K = x W_K,\ V = x W_V\)
- \(\mathrm{Attn}(Q,K,V) = \mathrm{softmax}(QK^\top / \sqrt{d_k}) V\)

直觉：

- \(QK^\top\)：当前 token「问」哪些历史 token「相关」
- softmax：权重归一化
- 乘 \(V\)：按权重混合信息
- \(\sqrt{d_k}\)：防止点积过大导致 softmax 过尖

**复杂度**：序列长 \(n\)、维度 \(d\)，注意力是 \(O(n^2 d)\)。所以长上下文贵，工程上才有窗口注意力、MLA、线性注意力等变体。

### 2.2 多头注意力（MHA）与变体

| 变体 | 要点 | 为何存在 |
|------|------|----------|
| MHA | 每头独立 Q/K/V | 表达力强，KV 显存大 |
| MQA | 所有头共享 K/V | KV 最小，质量可能掉 |
| GQA | 分组共享 K/V | Llama/Qwen 常见折中 |
| MLA | 低秩压缩 KV（DeepSeek） | 长上下文时省 KV |

面试金句：

> 推理显存往往不是「权重」先爆，而是 **KV Cache 随 batch 和序列线性涨**。GQA/MLA 就是在打 KV。

### 2.3 因果掩码（Causal Mask）

Decoder-only 训练/推理时，位置 \(i\) **不能看** \(i\) 之后的 token，否则「偷看未来」。这就是自回归语言模型的约束。

---

## 3. 位置编码：RoPE 为什么常考

绝对位置：直接把「第 3 个位置」加到向量上。  
**RoPE**：对 Q/K 做旋转，相对距离体现在旋转角差上，外推性更好。

追问：

- **YaRN / NTK-aware / 位置插值**：把训练长度（如 8K）扩到 32K/128K 的位置外推技巧。
- **外推 ≠ 真的会用超长上下文**：中间迷失（Lost in the Middle）仍然存在，所以应用层还是要 RAG + 裁剪。

---

## 4. FFN、Norm、残差

- **FFN**：逐位置的「知识存储/变换」，参数量通常大于注意力。MoE 就是把 FFN 变成「多个专家，每次只激活几个」。
- **残差**：保证深层可训练。
- **RMSNorm**：不算均值，只做均方根缩放，算得快、稳。

---

## 5. 生成：从 logits 到句子

最后一层输出词表大小的 logits → softmax → 采样：

| 参数 | 作用 | 面试注意 |
|------|------|----------|
| temperature | 越大越随机 | 0 ≈ 贪心，分析/SQL 宜低 |
| top-p (nucleus) | 只在累积概率 p 内采样 | 比单纯 top-k 更常用 |
| top-k | 只看前 k | 可与 top-p 组合 |
| repetition penalty | 抑制重复 | 过强会伤害专有名词 |
| max_tokens | 最大新生成长度 | 必须满足 input + max_tokens ≤ 上下文窗 |

本项目里客服/分析会动态压低 `max_tokens`，避免顶满 vLLM 上下文窗。

**预填充 vs 解码（Prefill / Decode）**：

- Prefill：一次吃完整 Prompt，算全部 QKV，**算力密集**
- Decode：每步一个新 token，反复读 KV，**显存带宽密集**

所以「首字时延 TTFT」和「每字时延 TPOT」优化手段不同。详见 [05](./05-推理加速与服务化.md)。

---

## 6. 架构族谱（能分类即可）

| 路线 | 代表 | 特点 |
|------|------|------|
| Encoder-only | BERT | 双向，适合分类/嵌入，不擅长开放生成 |
| Encoder-Decoder | T5、历史翻译模型 | 适合转换任务 |
| Decoder-only | GPT、Llama、Qwen | 生成主流 |
| MoE | Mixtral、DeepSeek-V3 | 总参大、激活参小，路由到专家 |
| 推理时增强 | DeepSeek-R1、Qwen3 thinking | 加 CoT / RL，测的是推理链 |

**嵌入模型**（bge、GTE、Qwen3-Embedding）本质常是 Encoder 或双向结构，和聊天 LLM **不要混用同一套「生成模型当向量」**（除非模型明确支持 embedding 接口）。

本项目：聊天走 vLLM Chat；向量/重排走 MIS-TEI（或本地 sentence-transformers）。

---

## 7. Scaling、能力边界与其它架构（行业）

- **Chinchilla 法则**：给定算力，参数量和训练 token 要匹配；「只堆参数少看数据」会欠训。现代叙事更强调 **高质量 token 预算**。  
- **Emergent / 涌现** 常被媒体夸大：很多是度量非线性，不是魔法开关。  
- 能力边界：知识截止日期、幻觉、符号计算弱、私有知识必须外挂。  
- **上下文窗**是硬约束；「支持 128K」≠ 该塞 128K，见 [18](./18-长上下文记忆与测试时计算.md)。

其它序列架构（认得即可）：

| 路线 | 一句话 |
|------|--------|
| SSM / Mamba | 线性复杂度，长序列省算力，指令生态仍弱于 Transformer |
| 线性 / 稀疏注意力 | 降 \(n^2\)，远距离质量要验证 |
| 混合（Attn+SSM） | 研究与部分新模型在拼 |

嵌入几何：对比学习让正对靠近、负对远离；**各向异性**时余弦失效，所以聊天模型隐状态不宜直接当向量。详见 [22](./22-嵌入向量库与检索引擎.md)。

---

## 8. 高频问答

**Q：Self-Attention 和 Cross-Attention 区别？**  
A：Self 的 Q/K/V 都来自同一序列。Cross 的 Q 来自解码端，K/V 来自编码端（翻译/多模态里常见：文本 Q 看图像 K/V）。

**Q：为什么除以 \(\sqrt{d_k}\)？**  
A：维度升高后点积方差变大，softmax 趋近 one-hot，梯度消失。缩放把方差拉回稳定区。

**Q：BERT 和 GPT 训练目标差在哪？**  
A：BERT 是 MLM（双向上下文填空）+ 可选 NSP；GPT 是因果语言建模（只看左边）。所以 BERT 适合理解/向量，GPT 适合生成。

**Q：什么是 KV Cache？**  
A：解码第 t 步时，前 t-1 步的 K/V 已算过。缓存下来，新步只算当前 token 的 Q/K/V 并与历史拼接。用空间换时间，显存随「层数 × 序列 × batch × 头维」增长。

**Q：MoE 为什么省？**  
A：总参数可以很大（记忆容量），但每个 token 只激活少量专家，**训练/推理 FLOPs 按激活参数算**。代价是路由不均、通信、专家并行更复杂。

**Q：幻觉的本质？**  
A：模型学的是「像不像训练分布」，不是「是否为真」。没有检索/工具时，会流畅地编造。治理见 [07][11]。

---

## 9. 追问与陷阱

- 说不清 MHA/GQA：回去补 KV 显存账。  
- 把「参数量」和「激活量」当成一回事：MoE 场景会被问穿。  
- 认为「上下文越长越好」：忽略 TTFT、丢失中间、费用。  
- Chat 模板错了还怪模型：先查 tokenizer apply_chat_template。

---

## 10. 速记口诀

> **切词 → 嵌入+位置 → 多层（注意力看历史 + FFN 变换）→ 预测下一 token。**  
> **训练看左边；推理缓存 KV；长文本先裁再算。**
