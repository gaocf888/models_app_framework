# 智能客服意图识别 · BERT（已废弃）

> **2026-09-14**：意图后端已收敛为 **`rules` | `funnel`**。  
> `CHATBOT_INTENT_BACKEND=bert` 与微调 BERT 路径 **已删除**。口语覆盖请使用 `funnel`（复用现有 Embedding + vLLM 主对话）。  
> 详见 `docs/基于地降所项目改造/AI问答改造.md` §5.4。
