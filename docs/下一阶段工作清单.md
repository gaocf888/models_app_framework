下一阶段工作清单（生产级补强 TODO）
一、LangChain / LangGraph 编排与 LangSmith 集成
TODO-P1：统一 LLM 推理服务 + LangChain 接入

实现 LLMInferenceService + api/llm_inference.py：
支持：纯 prompt、chat messages、模型 ID、Prompt 版本 ID、是否启用 RAG/上下文等参数。
在 LLMInferenceService 内使用 LangChain：
用 LLMClient 作为 LangChain LLM 后端；
将 PromptTemplateRegistry 接入链路（按 scene=llm_inference 获取模板）。
TODO-P2：Chatbot/Analysis/NL2SQL 的编排升级（Analysis 已切换 LangGraph V2）

Chatbot：
用 LangChain 构建 chatbot_chain（LLM + RAG + Memory），替换当前占位回答逻辑；
调用前从 PromptTemplateRegistry 获取 chatbot 场景下的 Prompt。
综合分析：
用 LangGraph/Agent 构建多步 Workflow（RAG 检索 + 工具调用 + 报告生成）；
接入 PromptTemplateRegistry 的 analysis 场景模板。
NL2SQL：
将当前 NL2SQLChain 重构为 LangChain Runnable/Chain；
使用 PromptTemplateRegistry 的 nl2sql 场景模板。
TODO-P3：LangSmith 集成

在上述三条链路中增加 LangSmith trace 上报（可通过环境变量开启/关闭）：
记录输入、Prompt 版本、模型 ID、RAG 命中等关键信息；
为后续对话质量与 NL2SQL 正确率评估提供数据。
二、会话与 RAG 持久化（Redis + 向量库）
TODO-P4：Redis 会话存储实现

实现 Redis 版 ConversationStore：
append_message / get_recent_history / clear / TTL 管理；
简单摘要/截断策略（如定长窗口 + 周期性压缩）。
在配置中提供切换开关（use_redis_conversation_store），保证能回退到内存实现。
TODO-P5：RAG 向量库生产化

为 VectorStoreProvider 增加至少一个真实实现（优先选 FAISS 或 pgvector）：
抽象接口不变，实现 add_texts / similarity_search_by_vector。
新增配置项：
rag.vector_store_type（如 faiss / pgvector / es）；
向量库连接信息（pgvector/ES 地址等）。
TODO-P6：RAG 摄入服务与 API（对应 TODO-5）

实现 RAGIngestionService：
文档/Schema/业务知识/问答样例的分片、嵌入与写入向量库；
维护数据集/命名空间元信息。
实现 api/rag_admin.py：
/rag/ingest：上传或注册新知识源；
/rag/datasets：查询当前知识库状态（命名空间、文档数、更新时间等）。
三、小模型训练与 TensorBoard 集成
TODO-P7：小模型训练流程落地

在 SmallModelTrainingService 中：
为至少一个示例算法（如 helmet_detector）实现实际训练循环（PyTorch）；
读取 SmallModelMeta.weights_path 作为初始权重路径（或保存路径）。
集成 TensorBoard：
使用 SummaryWriter 写入 loss、accuracy 等指标；
在配置中指定 log_dir。
TODO-P8：训练任务管理与（内部）API

扩展 training_service.py 或新增统一 TrainingManager：
维护训练任务 ID、状态（running/finished/failed）、开始/结束时间、日志路径；
提供查询接口（可在 api/train_admin.py 中暴露，只限内部/运维使用）。
四、大模型训练/微调管理闭环
TODO-P9：训练任务生命周期与可视化协同
在 LLaMAFactoryAdapter 和 LLMTrainingService 之外，新增统一的 Training Orchestrator：
start_llm_training(job_id, cfg, mode="factory"/"code")；
记录训练配置与输出路径；
支持查询训练状态（调用 LLaMA-Factory API 或读取任务元数据）。
api/train_admin.py：
POST /train/llm/start：启动训练任务；
GET /train/llm/status：查询训练任务状态列表/详情。
五、NL2SQL Schema 同步与 RAG 深化
TODO-P10：SchemaMetadataService 数据库同步

使用 DatabaseConfig 连接实际 aishare MySQL：
读取表名、字段、类型、注释、主外键等；
提供 refresh_schema() 方法；
api/nl2sql_admin.py 中新增 /nl2sql/schema/refresh 接口供运维调用。
TODO-P11：NL2SQL RAG 索引构建

在 NL2SQLRAGService 中：
设计命名空间（schema / biz_knowledge / qa_examples）；
提供从 Schema/业务知识源构建向量索引的流程；
在 NL2SQL 查询前使用这些命名空间进行检索（而不是仅用通用 RAG）。
六、性能与稳定性专项（需要结合实际环境）
TODO-P12：压测脚本与基准测试

为以下接口编写压测脚本（locust/JMeter 或自定义脚本）：
/llm/infer（待实现）。
/chatbot/chat。
/analysis/run-with-payload。
/analysis/run-with-nl2sql。
/nl2sql/query。
/small-model/channel/* + 实际视频源。
输出性能基准文档：
TPS/QPS、P95/P99 延迟、资源占用（CPU/GPU/内存）的测试结果；
在 docs/ 下新增一篇 性能与稳定性评估.md。
TODO-P13：针对瓶颈的优化行动

根据压测结果，在以下方面实施调优并记录：
LLM 调用层（连接池、批处理、超时/重试策略）。
小模型通道（线程数、队列容量、解码/推理频率、GPU 绑定策略）。
NL2SQL 查询（SQL 优化、索引、连接池）。